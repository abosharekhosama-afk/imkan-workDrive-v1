"use client";
import {useMemo,useState} from "react";
import type {WorkflowDynamicValue} from "@/lib/api/workflows";
export function WorkflowDynamicValuePicker({ar,values,onInsert}:{ar:boolean;values:WorkflowDynamicValue[];onInsert:(value:string)=>void}){const [open,setOpen]=useState(false);const [q,setQ]=useState("");const filtered=useMemo(()=>values.filter(v=>`${v.path} ${v.label}`.toLowerCase().includes(q.toLowerCase())).slice(0,30),[values,q]);return <div className="relative mt-1.5"><button type="button" className="wd-pill wd-pill-record w-full justify-between" onClick={()=>setOpen(v=>!v)}><span>{ar?'إدراج قيمة ديناميكية':'Insert dynamic value'}</span><span>⌄</span></button>{open&&<div className="wd-menu absolute z-[180] mt-1 w-full p-2"><input autoFocus value={q} onChange={e=>setQ(e.target.value)} className="wf-input mb-2" placeholder={ar?'ابحث في المتغيرات…':'Search variables…'}/><div className="max-h-56 overflow-y-auto space-y-1">{filtered.map(v=><button key={v.path} type="button" onClick={()=>{onInsert(`{{${v.path}}}`);setOpen(false)}} className="wd-menu-item min-h-[49px] py-2 text-start"><div className="text-[10px] font-semibold text-slate-700">{v.label}</div><div className="text-[8.5px] text-slate-400">{`{{${v.path}}}`} · {v.type}</div></button>)}{!filtered.length&&<div className="p-3 text-center text-[9px] text-slate-400">{ar?'لا توجد نتائج':'No results'}</div>}</div></div>}</div>}
