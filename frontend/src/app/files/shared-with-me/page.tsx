"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
import { listSharedWithMe, type SharedItem } from "../../../lib/api/shared";
import { useLocale } from "../../../components/locale-provider";
export default function SharedWithMePage(){ const {label}=useLocale(); const [rows,setRows]=useState<SharedItem[]>([]); const [error,setError]=useState<string|null>(null); useEffect(()=>{ let active=true; void listSharedWithMe().then(v=>{if(active)setRows(v)}).catch(()=>{if(active)setError(label("error.generic"))}); return()=>{active=false}; },[label]); return <section className="space-y-5"><div><p className="imkan-meta">Workspace</p><h1 className="text-xl font-semibold">Shared with me</h1></div>{error?<div className="imkan-alert">{error}</div>:<div className="imkan-panel overflow-x-auto"><table className="imkan-table"><thead><tr><th className="px-3 py-2 text-start">Resource</th><th className="px-3 py-2 text-start">Permission</th><th className="px-3 py-2 text-start">Expires</th></tr></thead><tbody>{rows.map(r=><tr className="imkan-table-row" key={r.id}><td className="px-3 py-2">{r.resourceType==="FOLDER"?<Link href={`/files/${r.resourceId}`}>{r.resourceId}</Link>:r.resourceId}</td><td className="px-3 py-2">{r.permission}</td><td className="px-3 py-2">{r.expiresAt?new Date(r.expiresAt).toLocaleString():"—"}</td></tr>)}{rows.length===0?<tr><td colSpan={3} className="px-3 py-8 text-center imkan-muted">No shared items.</td></tr>:null}</tbody></table></div>}</section> }
