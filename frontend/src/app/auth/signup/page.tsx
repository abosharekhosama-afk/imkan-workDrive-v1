"use client";
import Link from "next/link"; import {useState,type FormEvent} from "react"; import {useRouter} from "next/navigation"; import {googleUrl,saveSession,signup} from "../../../lib/api/auth"; import {useLocale} from "../../../components/locale-provider";
export default function SignupPage(){const router=useRouter();const{label}=useLocale();const[name,setName]=useState("");const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError("");try{saveSession(await signup(name,email,password));router.replace("/files")}catch(err){setError(err instanceof Error&&err.message?err.message:"Unable to create your account. The email may already be registered.")}finally{setBusy(false)}}async function google(){setBusy(true);try{window.location.href=(await googleUrl()).url}catch{setError("Google sign-in is not configured.");setBusy(false)}}return <main className="auth-page"><section className="auth-card"><div className="auth-brand"><span className="auth-logo">I</span><div><strong>IMKAN WorkDrive</strong><span>Secure cloud workspace</span></div></div><div className="auth-heading"><p className="imkan-meta">Create your workspace</p><h1>{label("auth.createAccount")}</h1><p>{label("auth.signupDescription")}</p></div><form onSubmit={submit} className="auth-form"><label>{label("auth.name")}<input className="imkan-input" required value={name} onChange={e=>setName(e.target.value)} autoComplete="name"/></label><label>{label("auth.email")}<input className="imkan-input" type="email" required value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email"/></label><label>{label("auth.password")}<input className="imkan-input" type="password" required minLength={8} value={password} onChange={e=>setPassword(e.target.value)} autoComplete="new-password"/></label>{error&&<div className="imkan-alert imkan-alert-danger">{error}</div>}<button className="imkan-button auth-submit" disabled={busy}>{busy?"Creating…":label("auth.createAccount")}</button></form><div className="auth-divider"><span>or</span></div><button className="google-button" onClick={google} disabled={busy}><span>G</span> {label("auth.continueGoogle")}</button><p className="auth-switch">{label("auth.haveAccount")} <Link href="/auth/login">{label("auth.signIn")}</Link></p></section></main>}
