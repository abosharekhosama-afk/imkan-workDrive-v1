import { Controller, Get } from '@nestjs/common'; import { CurrentUser } from '../auth/current-user.decorator'; import type { AccessTokenPayload } from '../auth/jwt.types'; import { QuotaService } from './quota.service'; @Controller('storage/quota') export class QuotaController {constructor(private readonly s:QuotaService){} @Get() get(@CurrentUser()u:AccessTokenPayload){return this.s.get(u)}}
