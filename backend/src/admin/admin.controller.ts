import { Controller, Get } from '@nestjs/common'; import { CurrentUser } from '../auth/current-user.decorator'; import type { AccessTokenPayload } from '../auth/jwt.types'; import { AdminService } from './admin.service'; @Controller('admin') export class AdminController {constructor(private readonly s:AdminService){} @Get('overview') overview(@CurrentUser()u:AccessTokenPayload){return this.s.overview(u)} @Get('users') users(@CurrentUser()u:AccessTokenPayload){return this.s.users(u)}}
